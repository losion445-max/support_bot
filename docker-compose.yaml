
services:
  bot:
    build:
      context: ./application
    # ports:
    #   - "8080:8080"
    network_mode: "host" 
    cap_add:
      - NET_ADMIN
      - NET_RAW
    dns:
      - 8.8.8.8
      - 1.1.1.1
    env_file:
      - .env